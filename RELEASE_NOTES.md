TheLoom v0.56.0 · 自动同步、地图底图瘦身与十模块巡检修复

**外链网盘自动同步**
- 同步面板新增「自动同步」开关（默认关）：停手 30 秒自动上传一次，回到窗口时检查远端
- 只自动上传，不自动替换——远端有更新只提示，拉取仍需你确认
- 绑定开启时的那部作品：远端只存一个项目对象，换作品后自动上传即停止，避免覆盖另一本
- 撞上冲突会暂停并提示，在面板里处理完自动恢复

**地图模块**
- 没有底图也能直接用：空白格纸上摆放地点标记、绘制区域与形状，导出 PNG 同样可用；美术可以以后再补
- 底图改存资源库（按内容哈希落盘），不再内联进项目文件——实测一张 319 KB 底图使项目体积从 358 KB 降到 39 KB
- 旧项目的内联底图在打开地图时自动搬运；搬运确认成功才清除内联那份，搬不动则保持原样

**数据安全修复**
- 修复更改文档块类型时静默丢失内容：列表条目、选项、条件与指令表达式此前只要换一次类型就会消失
- 修复更改块类型会切断与流程节点的内容共享：对白改成动作等同类改动现在保留链接
- 修复流程节点复制粘贴时副本与原件共享内容（改副本会同时改原件），子流程内部的节点标识、技术名同样逐层重铸
- 修复更改变量名时，跨流程实参、返回值表达式与外部事件实参不跟随改名的问题——此前既不改也不报错
- 缩略图剥离改为确认事务提交后才执行，杜绝配额超限场景下的缩略图丢失
- 「清理未引用原文件」在读不全其他作品的正文时整个拒绝执行，避免误删其他作品仍在使用的文件

**流程与体检**
- 演出、路径遍历与独立运行库的出边规则收敛为同一份实现，消除三处手工同步的隐患
- 修复演出中从命名出口返回父层时，分支可能走不通的问题
- 伏笔台账新增「缺埋设」状态：有回收记录却无埋设记录不再被标为「已回收」

**体验修复**
- 切换作品后，文档、流程与角色弧线面板不再停在空白，自动归位到可用内容
- 删除场景后落到相邻场景；删除实体与变量前列出会波及的引用处数
- 资料模块搜索改为忽略大小写与首尾空格，并纳入来源字段，与其他模块口径一致
- 修复删除资料分类后，卡片在任何分类筛选下都不出现的问题
- 头脑风暴画布：点选便签不再触发整个项目的保存

---

本版共修复 16 处问题，新增 56 项自动化测试（合计 643 项）。

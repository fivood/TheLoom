TheLoom v0.58.0 · 多作品同步、桌面端直连修复与移动端打磨

**外链网盘改为多作品文件夹格式同步**
- 远端不再是全局唯一的一个加密整包，改为按作品分目录，存的就是桌面版写在磁盘上的那套结构：`projects/{作品名}/project.json`、`documents/*.md`、`entities/*.md`
- 同步面板新增「查看远端作品」：列出远端全部作品与各自的文件数、体积、更新时间，可「拉到新槽位」或「覆盖当前」
- 多部作品从此互不覆盖。此前远端只有一个对象，有几本就得配几个不同的桶内路径
- 逐文件比对：改一个场景只上传那一个 .md；冲突也落到文件粒度，只有两边都改了同一个文件才会拦下，各改各的场景互不影响
- 项目正文改为明文存储，同一份文件可被 Obsidian 等工具直接读取；灵感库与资源原文件仍加密
- 没起名的作品拒绝上传：远端按作品名分目录，两本都叫「未命名项目」会互相覆盖

**桌面版同步连不上的问题已修复**
- 桌面版的存储请求改走本机转发，不再经过内嵌浏览器。此前受同源策略限制而直接失败，表现为「连不上」
- 因此桌面版**不需要**在存储桶上配置 CORS。旧版界面上「桌面版无此要求」的说明当时并不成立，现已更正

**移动端**
- 同步面板等弹层不再被底部标签栏遮挡，滚到底能看到全部按钮
- 块操作条下方多出的空白已消除；横向排不下的按钮两端加淡出提示，不再断在半截
- 深色主题下不再对品牌标识做反色处理

**修复**
- 更改文档块类型不再静默丢失内容：列表条目、选项、条件与指令表达式此前换一次类型就会消失
- 更改块类型不再切断与流程节点的内容共享；流程节点复制粘贴时，副本与原件不再共用同一份内容
- 更改变量名时，跨流程实参、返回值表达式与外部事件实参会一并跟随
- 演出中从命名出口返回上层时的分支走向已修正
- 伏笔台账新增「缺埋设」状态：有回收记录却无埋设记录不再显示为「已回收」
- 地图不再必须先有底图；底图改存资源库，不再撑大项目文件
- 切换作品后，文档、流程与角色弧线面板会自动归位到可用内容
- 缩略图与原文件清理增加多重保护，避免在异常情况下误删

---

自动化测试 663 项，较上一版新增 76 项。

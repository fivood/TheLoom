TheLoom v0.64.0 · 设定集总览、分屏联动与一批界面整理

**设定集:先摆骨架，再填内容**
- 新增「总览」视图：每个分类一张卡，显示条目数与「N / M 写了简介」的填充度；没写简介的条目带一个小点
- 空分类会一直留在总览里提醒你去填 —— 可以先摆出 魔法体系 / 势力 / 地理 / 历史，再逐个写
- 每张卡有「＋ 新建到这一类」，新建的条目直接归到该分类
- 工具栏新增「分组方式」：按类型 / 按分类（文件夹）/ 按模板 / 不分组。内置的五种类型是通用骨架，作品自己的分类用文件夹或模板
- 卡片视图也按同一分组分节显示

**分屏**
- 副面板已经开着某个模块时，跳转不再把主面板一起切走 —— 左边写、右边查的用法现在真的成立
- 分屏默认配对按预设来：正文↔设定集（游戏类是流程）、大纲 / 时间线 / 规划↔正文
- 副面板标签栏新增「与主面板互换」
- 同一个模块不能在两侧同时打开（副面板里该模块的标签会置灰；从左侧点它则两边对调）

**阶段开关**
- 写 / 改 / 理 / 设 改为图标，图标沿用各自主模块的图标
- 切阶段会同时把左侧切到该阶段的主模块：写 / 改 → 正文（游戏类是流程），理 → 大纲，设 → 设定集。此前点「写」左侧不动，看着像没生效
- 跳转记在历史里，顶栏「返回上一位置」可退回

**规划**
- 写作进度在没有场景时不再摆满 0，改为一句说明加「去写第一场」
- 「统计口径」从最显眼的标题位置挪到目标树面板；标题与统计卡整体收紧
- 节奏图、登场统计的空状态补上说明与跳转按钮
- 修掉引导文案指错地方的问题：小说预设下原本写着「实体」「文档」模块，实际按钮叫「设定集」「正文」

**界面**
- 提示文案里被「」圈出的界面元素名统一着色，一眼能看出哪几个字是要去点的东西
- 顶栏工具菜单重新分组：查找与检查 / 项目数据 / 素材与模板 / AI / 导出 / 导入；「检查更新」与版本号移到左上角图标菜单，不再重复

---

自动化测试 684 项，较上一版新增 7 项。

TheLoom v0.63.2 · 修复左上角图标菜单弹不出来

上一版的图标菜单被侧栏裁掉了：侧栏为了在矮窗口里能滚动，设了 overflow 隐藏，而菜单是贴着侧栏右侧绝对定位的，整块被切没。现在改为固定定位，坐标在打开时按按钮位置实测，不再受侧栏裁剪影响。

---

自动化测试 676 项。

TheLoom v0.63.1 · 左上角图标可以点了

图标看起来像个按钮却点不动，现在它真的是按钮：点开是一个应用菜单，放的是不属于任何模块的几个入口。

- **项目总览**（Ctrl+Shift+K）
- **全局搜索**（Ctrl+K）
- **使用指南**（F1）
- **检查更新**（仅桌面版）
- 底部显示当前版本与运行形态（网页版 / 桌面版）

菜单向右弹出，Esc 或点别处关闭。

---

自动化测试 676 项。

TheLoom v0.63.0 · 新增「设」阶段：任何作品都能切进世界观设定

世界观设定不是一种作品类型，而是每种写作都要做的一件事：写小说要翻人物关系，写剧本要查地点，跑团更是。所以它从「预设」挪到了「阶段」这条轴上。

**顶栏阶段开关变成四档：写 / 改 / 理 / 设**
- **设** = 设定集、地图、时间线、规划（关系图）、资料 提到最前，查世界观不必离开当前作品，切回「写」原样回来
- 阶段开关现在对**除「通用」外的全部预设**可见——互动叙事与 TRPG 模组此前没有阶段，现在也有
- 阶段仍然只重排导航、不增删模块，也不写入作品（本机设置）

**「设定集」预设保留**
- 它的适用场景是「整部作品本身就是设定集 / 世界观圣经」，正文才是附属
- 只是写小说时要查设定，用顶栏的「设」阶段就够了，不必把项目改成设定集类型

---

自动化测试 676 项，较上一版新增 1 项。

TheLoom v0.62.3 · 欢迎页选了布局就一定生效

**问题**:欢迎页选好创作类型后，如果点「跳过引导」、按 Esc 或点面板外关闭，选择会被丢掉，项目仍是「通用」布局——看起来像是没设置成功。

- 选中后下方直接显示「已选「小说」布局 · 之后可在项目菜单 ▾ 里随时更换」
- 底部按钮改为「使用「小说」布局并关闭」，点它就确认并关闭
- 按 Esc 或点面板外关闭，同样按所选布局生效，不再静默丢弃

---

自动化测试 675 项。

TheLoom v0.62.2 · 新建项目时直接选预设

**问题**:预设只在第一次使用的欢迎页问过一次，之后新建项目不再询问；想改预设要点开顶栏项目名旁的 ▾，而那里的标题写的是「工作区布局」，不容易和「预设」对上号。

- 项目菜单 →「新建空白项目…」改为两步：先列出七套预设（带一句说明），选完再创建
- 新项目立刻套用所选预设，左侧导航当场变
- 抽屉里的标题改为「工作区预设（随时可改）」，明确它任何时候都能换
- 「新建 · 载入示例」与「从 JSON 导入」不变

---

自动化测试 675 项。

TheLoom v0.62.1 · 剧本:转场块与场景标题改名

**新增「转场」块**
- 正文里 ＋ → 转场，写 CUT TO: / 淡出 / 叠化，单行右对齐
- 导出 Final Draft 时是正规的 Transition 段落；导入 .fdx 时 Transition 也会还原成转场块（此前落成一条注释）
- 成稿导出（DOCX）有独立的右对齐转场样式；Markdown / TXT 编译、沉浸写作、版本差异、文件夹模式的 Markdown 都能无损往返
- 转场不进流程，与子标题、引用同类

**块名跟着预设走**
- 剧本 / TRPG 预设下，「场景锚点」显示为「场景标题」
- 小说等其余预设不变

---

自动化测试 675 项，较上一版新增 2 项。

TheLoom v0.62.0 · 新增四套工作区预设

预设从三套增加到七套。预设只改模块顺序、命名与界面密度，不锁任何功能——随时可以切回「通用」看到全部模块，数据一件不少。

**剧本（影视 / 舞台）**
- 首层：剧本、场次表、设定集、规划、资料
- 插入块以 场景锚点 / 动作 / 对白 为主（正文段落仍在「＋」菜单里）
- 正文页 ⋯ 菜单里直接有「导出 Final Draft」

**设定集（世界观 / 百科）**
- 首层：设定集、地图、时间线、规划、资料、正文
- 打开项目落在设定集，不再从正文进

**纪实（非虚构）**
- 首层：资料来源、大纲、正文、时间线、设定集、风暴
- 资料模块改名「资料来源」，打开项目落在这里

**TRPG 模组**
- 首层：流程、NPC 与道具、地图、变量、剧本、资源
- 打开项目落在流程

**顺带**
- 「写 / 改 / 理」阶段开关现在对小说、剧本、设定集、纪实四套都有效
- 「理」阶段的导航重排改为通用规则：大纲、时间线、规划提到最前，其余保持预设自身顺序

---

自动化测试 673 项，较上一版新增 1 项。

TheLoom v0.61.0 · 写 / 改 / 理 三档写作阶段

小说预设下，顶栏项目名右侧多了「写 / 改 / 理」开关。同一部作品在不同阶段要的界面不一样，这个开关只换界面，不动作品数据。

**写**
- 就是上一版的样子：只留正文，按钮按需展开

**改**
- 工具栏多出「查找替换」「存快照」「版本差异」
- 右侧属性栏自动展开：批注、快照列表、修订轮次、场景元数据都在手边

**理**
- 左侧首层换成 大纲 → 时间线 → 规划 → 设定集 → 正文 → 风暴
- 正文页自动进「结构」视图，按块查看、改类型、拖拽重排

**说明**
- 阶段记在本机，不写入作品，也不参与同步——台式机上改稿、笔记本上写初稿互不影响
- 阶段只改模块顺序与界面密度，不会隐藏任何数据；切回「写」或换预设，东西都在
- 互动叙事 / 通用预设不显示这个开关

---

自动化测试 672 项，较上一版新增 1 项。

TheLoom v0.60.0 · 写作界面减负

小说预设（新建项目时选「小说」）下，正文页只留写字要用的东西，其余按钮收起或按需出现。互动 / 通用预设不变。

**正文块**
- 段落块不再显示「正文」类型标签——小说里几乎每一块都是段落，标签是重复信息
- 块左侧的 ↑ ↓ 删除 平时完全隐藏，鼠标移到该块或光标落在该块时才出现
- 底部插入栏收成一个 ＋ 按钮（展开仍是全部八种块）。Enter 本来就是新段落

**工具栏**
- 从 8 项减到 5 项：写作 / 结构 / 连续稿、专注、属性、⋯、搜索
- 「长篇工具」「新建分类」移入 ⋯ 菜单
- 没有建过分类的项目不再显示分类下拉（所有预设生效）

**右侧属性栏**
- 可折叠，工具栏「属性」按钮开关，小说预设默认收起
- 开关状态记在本机，不写入作品

**专注模式**
- 保留左侧场景列表，切场景不必退出专注；可用「收起列表」隐藏

**手机 / PWA**
- 键盘上方那条操作条从 10 个按钮减到 7 个，去掉 ＋正文 / ＋动作 / ＋对白（Enter 新建段落，其余类型走「类型 ▾」菜单）
- 写作页顶栏去掉主题切换，主题开关在「我的」页

---

自动化测试 671 项。

TheLoom v0.59.1 · 手机端新建项目与改名（仅网页端）

**「我的」页**
- 项目名可直接点开修改，不必回到桌面端
- 新增「新建空白项目」，此前手机上只能切换已有项目
- 项目列表里当前项目的名字随改随更新

**写作页**
- 空场景提示区补上留白，文字与按钮不再贴着屏幕边缘

---

自动化测试 671 项。

TheLoom v0.59.0 · 顶栏一键同步、窗口尺寸记忆与使用指南重写

**同步不再需要翻菜单**
- 顶栏新增同步按钮，一次点击即上传当前作品；有未同步的改动时按钮上会出现红点
- 右键该按钮打开完整的同步面板（查看远端作品、拉取、覆盖等）
- 未配置存储时按钮不显示
- 顶栏同时新增「项目总览」入口，此前埋在工具菜单里

**桌面版窗口尺寸**
- 首次启动按当前显示器大小开窗，不再固定为一个小窗。4K 屏上不必每次拖大
- 之后记住你上次的尺寸、位置与是否最大化，下次照原样打开
- 换到更小的屏幕、或窗口曾被拖到屏幕外时，会自动收回可见范围内

**使用指南（F1）重写**
- 扩充为 10 节，新增「跨设备同步」与「检查与数据安全」两节
- 删去 3 处已不成立的说明（视频封面、AI 完整项目导入、节点类型数量）
- README 相应精简为概览，功能细节以应用内指南为准

**配色表**
- 取消颜色数量上限
- 新增示例文件 `examples/palette-sample.json`，可直接导入查看 JSON 格式

---

自动化测试 671 项，较上一版新增 8 项。
